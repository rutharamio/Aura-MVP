<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 🔐 Permisos necesarios -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Aura">

        <!-- 🗺️ Clave de Google Maps -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <!-- 🚀 Pantalla de bienvenida -->
        <activity
            android:name=".ui.WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 🗺️ Pantalla principal del mapa -->
        <activity android:name=".MainActivity" android:exported="false" />

        <!-- 🔑 Pantalla de inicio de sesión -->
        <activity android:name=".ui.LoginActivity" android:exported="false" />

        <!-- 📝 Pantalla de registro -->
        <activity android:name=".ui.RegisterActivity" android:exported="false" />

        <!-- 📇 Módulo de contactos -->
        <activity android:name=".ui.AddContactActivity" android:exported="false" />
        <activity android:name=".ui.ContactListActivity" android:exported="false" />

        <!-- ⚙️ Solicitud de permisos -->
        <activity android:name=".ui.PermissionActivity" android:exported="false" />

        <!-- 🚨 Módulo de emergencia -->
        <activity android:name=".ui.EmergencyModuleActivity" android:exported="false" />

        <!-- ⚡ Receiver del botón de encendido -->
        <receiver
            android:name=".receivers.PowerButtonReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.SCREEN_ON" />
                <action android:name="android.intent.action.SCREEN_OFF" />
            </intent-filter>
        </receiver>

        <!-- 🆘 Servicio de emergencia -->
        <service
            android:name=".services.EmergencyService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location|dataSync" />
    </application>

</manifest>
